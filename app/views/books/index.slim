h1 Books List

= link_to 'New Book', new_book_path, class: 'btn btn-primary'

table.table
  thead
    tr
      th Title
      th Description
      th Cover Image
      th Published Date
      th Genre
      th Author
      th Actions

  tbody
    - @books.each do |book|
      tr
        td = book.title
        td = book.description
        td
          - if book.cover_image.present?
            = image_tag book.cover_image, size: "100x100", alt: book.title
          - else
            No image
        td = book.published_date.strftime('%B %d, %Y')
        td = book.genre
        td = book.author.name if book.author.present?
        td
          = link_to 'Show', book_path(book), class: 'btn btn-info'
          = link_to 'Edit', edit_book_path(book), class: 'btn btn-warning'
          = link_to 'Destroy', book_path(book), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger'
